#include <stdio.h>
int main() 
{
   int a[10],lar,n,i;
   printf("enter size of an array\n");
   scanf("%d",&n);
   printf("enter elements\n");
   for( i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
   }
   lar=a[0];
  
  for( i=0;i<n;i++)
   {
      if(lar<a[i])
      lar=a[i];
      }
   printf("largest number is%d",lar);
 return 0;
 
  }
