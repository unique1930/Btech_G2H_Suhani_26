#include <stdio.h>
int main() 
{
   int c=0,n,i,d;
   printf("enter number\n");
   scanf("%d",&n);
   
   while(n!=0)
   {
     d=n%10;
     c++;
     n=n/10;
   }
   printf("number of digits in a number is\n");
   printf("%d",c);
 return 0;
  }
  
