#include <stdio.h>
int main() 
 { 
     float s=0.0;
     int n;
    printf("enter number\n");
     scanf("%d",&n);
  for(int i=1;i<=n;i++)
    {
        s=s+(1.0/i);
    }
       printf("sum of series is %f",s);
 return 0;
}
