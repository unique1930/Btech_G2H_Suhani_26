#include <stdio.h>
int main() 
{
    int c=0;
    char t[50];
    printf("enter string \n");
    scanf("%s",t);
    for(int i=0;i<t[i];i++)
   {
        if(t[i]=='a'||t[i]=='e'||t[i]=='i'||t[i]=='o'||t[i]=='u')
        c++;
   }
    printf("vowels in a string is %d",c);
    return 0;
}
