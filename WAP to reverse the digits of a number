#include <stdio.h>
int main() 
{
   int rev=0,n,i,d;
   printf("enter number\n");
   scanf("%d",&n);
   
   while(n!=0)
   {
     d=n%10;
     rev=rev*10+d;
     n=n/10;
   }
   printf("reverse of a number is\n");
   printf("%d",rev);
 return 0;
  }
  
