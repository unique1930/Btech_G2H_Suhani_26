#include <stdio.h>
int main() 
{
   int a[10],b[10],n,i;
   printf("enter size of an array\n");
   scanf("%d",&n);
   printf("enter elements\n");
   for( i=0;i<n;i++)
   {
       scanf("%d",&a[i]);
   }
  for(i=0;i<n;i++)
  {
    b[i]=a[i];
  }
  printf("copy elements in array b are");
 for(i=0;i<n;i++)
 printf("%d\n",b[i]);
 
  return 0;
 
  }
  

  
