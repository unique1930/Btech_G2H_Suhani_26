#include <stdio.h>
int main() 
{
   int f,n,i;
   printf("enter number\n");
   scanf("%d",&n);
   printf("Factors are\n");
   for(i=1;i<=n;i++)
   {
       if(n%i==0)
      printf( "%d\n",i);
       }
    return 0;
  }
  
